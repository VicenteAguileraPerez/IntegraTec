<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vicenteaguilera.integratec" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".controllers.MainAdviserActivityApp"/>
        <activity android:name=".controllers.ListAdviserActivity"/>
        <activity android:name=".controllers.mainapp.MainAppActivity"/>
        <activity android:name=".controllers.ComplaintSuggestionsActivity"/>
        <activity android:name=".SplashScreenActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".controllers.OptionsActivity" />
         <activity android:name=".controllers.AboutActivity">

          </activity>
    </application>
</manifest>